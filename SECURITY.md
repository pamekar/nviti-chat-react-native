# Security

Report vulnerabilities privately to security@nviti.ng. Do not include production credentials or customer conversation data in reports.

The SDK accepts only HTTPS session URLs matching the configured origin. The host app must request signed sessions from its own trusted backend and explicitly allow every native capability.
